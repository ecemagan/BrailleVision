import { WordAddinWorkspace } from "@/components/WordAddinWorkspace";

export const metadata = {
  title: "Braille Vision for Word",
  description: "Modern Word add-in workspace for Braille and Nemeth conversion.",
};

export default function WordAddinPage() {
  return <WordAddinWorkspace />;
}
